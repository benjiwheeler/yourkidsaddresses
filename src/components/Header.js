import React from 'react';
import UpcomingBirthday from '../controllers/UpcomingBirthdays';

const Header = () => (
  <div className="banner bg-success">
    <UpcomingBirthday />
  </div>
);

export default Header;
